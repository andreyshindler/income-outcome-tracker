target-version = "py312"
line-length = 100

[lint]
# Pyflakes (F) + a focused pycodestyle/import set. Kept deliberately small to
# lint for real problems (unused imports/vars, undefined names, import order)
# without bikeshedding style.
select = ["E4", "E7", "E9", "F", "I"]
