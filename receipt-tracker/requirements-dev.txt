# Test-only deps. The Google Vision SDK and psycopg2 are NOT needed: tests stub
# google.cloud.vision and run against in-memory SQLite (see tests/conftest.py).
fastapi>=0.115,<0.116
httpx>=0.27,<0.29
sqlalchemy>=2.0,<2.1
pytest>=8,<9
