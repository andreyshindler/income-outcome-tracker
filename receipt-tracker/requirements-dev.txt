# Test-only deps. easyocr/torch and psycopg2 are NOT needed: tests stub
# ocr_image directly and run against in-memory SQLite (see tests/conftest.py).
fastapi>=0.115,<0.116
httpx>=0.27,<0.29
sqlalchemy>=2.0,<2.1
pytest>=8,<9
ruff>=0.15.20,<0.16
